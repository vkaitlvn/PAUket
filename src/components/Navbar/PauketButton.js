import React from "react";

import { Button } from "../ButtonElements2.js";

const PauketButton = () => {
  const Pauket = async () => {};

  // );

  return (
    <Button onClick={Pauket} className="pauket-button">
      LAUNCH APP
    </Button>
  );
};

export default PauketButton;
